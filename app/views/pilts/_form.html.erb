<%= form_for(@pilt) do |f| %>
  <% if @pilt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pilt.errors.count, "error") %> prohibited this pilt from being saved:</h2>

      <ul>
      <% @pilt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :picture %><br />
    <%= f.file_field :picture %>
  </div>
  <div class="field">
   <%= f.label :rating %><br />
   <%= f.select :rating, Pilt.possible_ratings %>
  </div
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
